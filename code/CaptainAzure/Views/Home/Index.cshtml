@{
    ViewData["Title"] = "Home Page";
}

<table class="table-hover claim-table">
    <tr>
        <th class="claim-type claim-data claim-head">Wisdom</th>
    </tr>

    @foreach (var key in Model)
    {
        <tr>
            <td class="claim-data">@key</td>
        </tr>
    }
</table>